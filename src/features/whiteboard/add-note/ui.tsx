import { ToggleButton, ToggleButtonProps } from 'shared/ui/toggle-button';

export const AddNote = (props: ToggleButtonProps) => (
  <ToggleButton {...props} variant="text">
    T
  </ToggleButton>
);
